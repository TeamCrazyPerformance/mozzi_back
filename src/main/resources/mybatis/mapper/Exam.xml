<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tcp.mozzi.back.mapper.exam.ExamMapper">
    <select id="getCount" resultType="int">
        select count(*)
        from exam
    </select>

    <select id="getExamList" resultType="com.tcp.mozzi.back.domain.exam.Exam">
        select *
        from exam
        limit #{page}, #{limit}
    </select>

    <insert id="createExam" >
        insert into (exam, author, create_at, year, major, grade, semester, term, name, professor, content)
        value (#{author}, #{create_at}, #{year}, #{major}, #{grade}, #{semester}, #{term}, #{name}, #{professor}, #{content})
    </insert>

    <update id="modifyExam">
        update exam
        set author = #{author}, year = #{year}, major = #{major}, grade = #{grade}, semester = #{semester}, term = #{term}, name = #{name}, professor = #{professor}, content = #{content}
        where exam_id = #{examId}
    </update>

    <select id="searchExam" resultType="java.util.HashMap">
        select *
        from exam
        where professor = #{professor} | name = #{name}
    </select>

    <delete id="deleteExam">
        delete from exam
        where exam_id = #{exam_id}
    </delete>
</mapper>