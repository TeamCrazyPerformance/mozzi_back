<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tcp.mozzi.back.mapper.user.UserMapper">
    <insert id="createUser" parameterType="com.tcp.mozzi.back.dto.user.UserDTO">
        INSERT INTO user
        VALUE (0, now(), now(), #{name}, #{password}, "WAIT", #{nickname}, #{studentNumber}, #{phoneNumber}, #{email}, #{birthday})
    </insert>
</mapper>