<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tcp.mozzi.back.mapper.user.UserMapper">
    <resultMap id="baseUserResultMap" type="com.tcp.mozzi.back.domain.user.User">
        <id column="user_id" property="userId" />
        <result column="create_at" property="createdAt" />
        <result column="modify_at" property="modifiedAt" />
        <result column="student_number" property="studentNum" />
        <result column="phone_number" property="phoneNum" />
    </resultMap>

    <select id="selectUserByUserId" resultMap="baseUserResultMap">
        SELECT user_id
              ,role
              ,create_at
              ,modify_at
              ,id
              ,password
              ,name
              ,status
              ,nickname
              ,student_number
              ,phone_number
              ,email
              ,birthday
              ,allow
        FROM user
        WHERE user_id = #{userId}
    </select>

    <select id="selectUserById" resultMap="baseUserResultMap">
        SELECT user_id
              ,role
              ,create_at
              ,modify_at
              ,id
              ,password
              ,name
              ,status
              ,nickname
              ,student_number
              ,phone_number
              ,email
              ,birthday
              ,allow
        FROM user
        WHERE id = #{id}
    </select>

    <select id="selectUsers" resultMap="baseUserResultMap">
        SELECT user_id
              ,role
              ,create_at
              ,modify_at
              ,id
              ,password
              ,name
              ,status
              ,nickname
              ,student_number
              ,phone_number
              ,email
              ,birthday
              ,allow
        FROM user
    </select>

    <insert id="insertUser" >
        INSERT INTO user (role, create_at, modify_at, id, password, name, status, nickname, student_number, major, phone_number, email, birthday)
        value ('user', now(), now(), #{id}, #{password}, #{name}, 'wait', #{nickname}, #{studentNum}, #{phoneNum}, #{major}, #{email}, #{birthday})
    </insert>

    <update id="updateUser">
        UPDATE user
        SET modify_at = now(), id = #{id}, password = #{password}, name = #{name}, status = #{status}, nickname = #{nickname}, student_number = #{studentNum}, phone_number = #{phoneNum}, email = #{email}, birthday = #{birthday}, allow = #{allow}
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteUserById">
        DELETE FROM user
        WHERE user_id = #{userId}
    </delete>

    <select id="isExistUserByName" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM user WHERE id = #{name})
    </select>
</mapper>
